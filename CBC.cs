using System;
namespace hw
{
    public class CBC
    {
        public static byte[] Encrypt(byte[] plain_text, byte[] key, byte[] iv)
        {
            // this is to test if git is working, yo ? how come, you are only pushing random stuff
            return plain_text; 
        }
    }
}
